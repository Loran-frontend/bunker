#!/usr/bin/env bash
set -e

echo "=========================================="
echo "🚀 Создание веб-игры «Бункер»..."
echo "=========================================="

mkdir -p bunker-game/public/css bunker-game/public/js bunker-game/src/data bunker-game/src/logic

cat << 'EOF' > bunker-game/package.json
{
  "name": "bunker-game",
  "version": "1.0.0",
  "description": "Bunker board game web application for online / local network",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node -e \"console.log('Setup test passed')\""
  },
  "dependencies": {
    "express": "^4.19.2",
    "socket.io": "^4.7.5"
  }
}
EOF

cat << 'EOF' > bunker-game/vercel.json
{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "server.js"
    }
  ]
}
EOF

cat << 'EOF' > bunker-game/src/data/cardsData.js
module.exports = {
  "professions": [
    "Астрофизик",
    "Профессиональный дегустатор корма для животных",
    "Мемный криптоинвестор",
    "Эксперт по выживанию в интернете",
    "ТикТокер-миллионник",
    "Плотник",
    "Хирург",
    "Повар молекулярной кухни",
    "Инженер ядерных реакторов",
    "Программист 1С",
    "Сантехник высшего разряда",
    "Фермер-гидропоник",
    "Снайпер в отставке",
    "Автомеханик",
    "Учитель начальных классов",
    "Биохимик",
    "Электрик",
    "Аниматор в костюме фиксика",
    "Дрессировщик боевых тараканов",
    "Стоматолог",
    "Психотерапевт",
    "Космонавт",
    "Философ-гитарист",
    "Бывший спасатель МЧС",
    "Тату-мастер",
    "Фокусник-иллюзионист",
    "Охотник на дичь",
    "Пилот гражданской авиации",
    "Архитектор бункеров",
    "Генетик",
    "Ветеринар",
    "Сварщик 6-го разряда",
    "Агент по недвижимости",
    "Киберспортсмен",
    "Диджей",
    "Библиотекарь",
    "Инструктор по йоге",
    "Машинист поезда",
    "Крановщик",
    "Метеоролог"
  ],
  "health": [
    "Абсолютно здоров",
    "Кастрирован",
    "Хроническая икота во сне",
    "Слепой на один глаз и глухой на второй",
    "Аллергия на кислород",
    "Сахарный диабет 1-го типа",
    "Навязчивое желание читать стихи при стрессе",
    "Астма в легкой форме",
    "Заикание при испуге",
    "Гипертония",
    "Плоскостопие 3-й степени",
    "Метеоризм от бобовых",
    "Бессонница",
    "Хронический гастрит",
    "Мигрень при ярком свете",
    "Синдром Туретта (легкая форма)",
    "Отсутствие аппендикса",
    "Сомнамбулизм (лунатизм)",
    "Аллергия на пыль и шерсть",
    "Псориаз на локтях",
    "Протез левой ноги",
    "Протез правого глаза",
    "Дальтонизм",
    "Куриная слепота",
    "Нарколепсия (внезапно засыпает)",
    "Зависимость от кофеина",
    "Отсутствие селезенки",
    "Хронический отит",
    "Ревматизм в суставах",
    "Нервный тик левого глаза",
    "Склонность к паническим атакам",
    "Аллергия на цитрусовые",
    "Анемия",
    "Повышенный метаболизм",
    "Алопеция (полное отсутствие волос)",
    "Болезнь движения (укачивает)",
    "Почечнокаменная болезнь",
    "Потеря вкуса и обоняния",
    "Стрессовое закладывание ушей",
    "Хронический бронхит"
  ],
  "biology": [
    "Мужчина 28 лет (плодовит)",
    "Женщина 45 лет (бесплодна)",
    "Мужчина 19 лет (чайлдфри со стажем)",
    "Женщина 22 года (высокая фертильность)",
    "Мужчина 65 лет (здоров, есть дети)",
    "Женщина 30 лет (беременна, 1 триместр)",
    "Мужчина 35 лет (донор спермы со стажем)",
    "Женщина 18 лет (мать тройни)",
    "Мужчина 50 лет (стерилизован)",
    "Женщина 26 лет (генетически идеальная)",
    "Мужчина 24 года (атлет)",
    "Женщина 40 лет (мастер спорта)",
    "Мужчина 70 лет (мудрец)",
    "Женщина 52 года (врач-акушер)",
    "Мужчина 31 год (двойняшка)",
    "Женщина 29 лет (аллергия на собственную гормональную терапию)",
    "Мужчина 42 года (отец 5 детей)",
    "Женщина 33 года (здорова, идеальные гены)",
    "Мужчина 21 год (студент)",
    "Женщина 25 лет (фитнес-тренер)",
    "Мужчина 38 лет (богатырское телосложение)",
    "Женщина 21 год (студентка медвуза)",
    "Мужчина 48 лет (ветеран)",
    "Женщина 36 лет (биолог)",
    "Мужчина 23 года (выносливый)",
    "Женщина 28 лет (полиглот)",
    "Мужчина 55 лет (опыт выживания)",
    "Женщина 24 года (гибкая)",
    "Мужчина 29 лет (высокий рост 205 см)",
    "Женщина 31 год (рост 150 см, компактная)",
    "Мужчина 34 года (крепкий иммунитет)",
    "Женщина 27 лет (марафонщица)",
    "Мужчина 40 лет (бывший военный)",
    "Женщина 39 лет (психолог)",
    "Мужчина 27 лет (инженер)",
    "Женщина 23 года (нутрициолог)",
    "Мужчина 33 года (фермер)",
    "Женщина 37 лет (эколог)",
    "Мужчина 22 года (химик)",
    "Женщина 20 лет (геолог)"
  ],
  "inventory": [
    "Набор ржавых отвёрток",
    "Упаковка просроченных презервативов",
    "Бензопила без бензина",
    "10 кг сушеной манги",
    "Оптический прицел без винтовки",
    "Живая курица по имени Коко",
    "Портативная солнечная батарея",
    "Фильтр для воды (ресурс 500л)",
    "Армейский сухпаек (10 упаковок)",
    "Мощный светодиодный фонарь",
    "Складной швейцарский нож",
    "Тепловизор (батарея 20%)",
    "Рация UHV/VHF (диапазон 5 км)",
    "Набор медицинских скальпелей",
    "Огнетушитель углекислотный",
    "Канистра очищенного бензина (5л)",
    "Набор для ловли рыбы",
    "Двуствольное ружье и 3 патрона",
    "Комплект костюма химзащиты (ОЗК)",
    "Лопата саперная",
    "Арбалет и 5 стрел",
    "Коробка сигнальных ракет (4 шт)",
    "Бронежилет 3 класса",
    "Топор туристический",
    "Пакет с семенами овощей (1 кг)",
    "Газовый баллончик (3 шт)",
    "Набор дозиметров (2 шт)",
    "Паяльник и припой",
    "Кевларовые перчатки",
    "Компрессор воздушный",
    "Стеклянная банка с маринованными огурцами",
    "Механические часы со светящимся циферблатом",
    "Набор для шитья и хирургические нити",
    "Прибор ночного видения",
    "Промышленный магнитофон",
    "Катушка медной проволоки (100м)",
    "Ультрафиолетовый стерилизатор",
    "Набор гаечных ключей",
    "Переносной складной стул",
    "Громкоговоритель на батарейках"
  ],
  "backpack": [
    "Зажигалка без газа",
    "Фотография чьей-то мамы",
    "Початая пачка жвачки",
    "Ножницы для ногтей",
    "Резиновая уточка",
    "Связка ключей от неизвестной двери",
    "Плитка горького шоколада (85%)",
    "Рулон туалетной бумаги",
    "Тетрадь в клеточку и шариковая ручка",
    "Карманное зеркальце",
    "Компас туристический",
    "Флешка на 64ГБ с мемами 2015 года",
    "Швейный набор",
    "Аспирин (2 блистера)",
    "Бутылка чистой воды 0.5л",
    "Суперклей (3 тюбика)",
    "Карманный фонарик-брелок",
    "Губная гармошка",
    "Колода игорных карт",
    "Пачка влажных салфеток",
    "Пара сухих шерстяных носков",
    "Крем от загара SPF 50",
    "Березовый деготь",
    "Зубная щетка и паста",
    "Пачка семечек подсолнуха",
    "Йод и бинт",
    "Беруши и маска для сна",
    "Пластиковый свисток",
    "Кусок мыла",
    "Эластичный бинт",
    "Пачка батареек АА (4 шт)",
    "Металлическая кружка",
    "Соль пищевая (200g)",
    "Спички водостойкие",
    "Бальзам 'Звездочка'",
    "Нитки и иголки",
    "Запасные очки (+2.0)",
    "Магниевый огниво",
    "Спиртовые салфетки (20 шт)",
    "Таблетки активированного угля"
  ],
  "phobias": [
    "Арахнофобия (страх пауков)",
    "Страх перед открытыми дверями холодильника",
    "Боязнь красивых женщин",
    "Гиппопотомонстросесквиппедалиофобия (страх длинных слов)",
    "Боязнь остаться без Wi-Fi",
    "Клаустрофобия (страх замкнутых пространств)",
    "Агорафобия (страх открытых пространств)",
    "Акрофобия (страх высоты)",
    "Никтофобия (страх темноты)",
    "Гермофобия (страх микробов)",
    "Пирофобия (страх огня)",
    "Гемофобия (страх крови)",
    "Трипофобия (страх кластерных отверстий)",
    "Офдиофобия (страх змей)",
    "Орнитофобия (страх птиц)",
    "Талассофобия (страх глубины/океана)",
    "Эмитофобия (страх тошноты)",
    "Социофобия (страх общения)",
    "Ауллофобия (страх флейт)",
    "Кулорофобия (страх клоунов)",
    "Автоматофобия (страх манекенов)",
    "Педиофобия (страх кукол)",
    "КеNodeTypeФобия (страх пустоты)",
    "Астрафобия (страх грозы)",
    "Мизофобия (страх грязи)",
    "Омброфобия (страх дождя)",
    "Сколецифобия (страх червей)",
    "Элакрофобия (страх кошек)",
    "Кинофобия (страх собак)",
    "Эйхофобия (страх выслушивать пожелания)",
    "Катагелофобия (страх быть высмеянным)",
    "Фобофобия (страх испытывать страх)",
    "Атаксарофобия (страх беспорядка)",
    "Акрибофобия (страх не понять смысл)",
    "Эритрофобия (страх покраснеть)",
    "Спектрофобия (страх зеркал)",
    "Трихофобия (страх волос)",
    "Хронофобия (страх времени)",
    "Теофобия (страх карающих богов)",
    "Ксенофобия (страх незнакомцев)"
  ],
  "skills": [
    "Умеет готовить кашу из топора",
    "Имеет разряд по шахматам",
    "Служил во флоте",
    "Умеет подделывать документы",
    "Имеет навыки первой помощи",
    "Знает основы радиоэлектроники",
    "Отличный стрелок из любого оружия",
    "Мастер бесшумного передвижения",
    "Умеет ориентироваться по звездам",
    "Знает 4 иностранных языка",
    "Умеет разводить огонь без спичек",
    "Опытный слесарь-сантехник",
    "Знает основы травничества и народной медицины",
    "Водит любой вид транспорта",
    "Мастер рукопашного боя",
    "Знает как строить укрепления",
    "Умеет консервировать продукты в полевых условиях",
    "Отличный переговощик и медиатор",
    "Умеет шить одежду и обувь",
    "Знает как очистить воду подручными средствами",
    "Мастер по ремонту электроники",
    "Умеет плести сети и тенета",
    "Знает устройство ядерных реакторов",
    "Профессиональный взломщик замков",
    "Умеет варить алкоголь из любого сырья",
    "Знает как обращаться с химикатами",
    "Опытный пчеловод",
    "Умеет разделывать туши животных",
    "Мастер плотницкого дела",
    "Знает основы психологии и манипуляций",
    "Умеет читать карты и схемы",
    "Опытный рыболов",
    "Знает основы сварки металлов",
    "Умеет играть на гитаре для поднятия духа",
    "Разбирается в грибах (съедобные/ядовитые)",
    "Умеет быстро считать в уме",
    "Знает как хранить запасы без холодильника",
    "Мастер точечного массажа",
    "Умеет чинить обувь",
    "Знает основы агрономии"
  ],
  "hobbies": [
    "Втайне пишет фанфики",
    "Победитель чемпионата по поеданию хот-догов",
    "Изгнан из прошлого бункера за кражу еды",
    "Умеет имитировать голоса птиц",
    "Коллекционирует пивные крышки",
    "Занимается медитацией по 2 часа в день",
    "Одержим идеей построить вечный двигатель",
    "Ведет дневник наблюдателя за облаками",
    "Увлекается оригами из газет",
    "Умеет показывать 50 фокусов с картами",
    "Регулярно моржевал в ледяной воде",
    "Увлекается резьбой по дереву",
    "Любит петь караоке без музыки",
    "Собирает пазлы на 5000 деталей",
    "Увлекается астрологией и гороскопами",
    "Плетет браслеты из паракорда",
    "Мечтает написать роман о зомби",
    "Увлекается историей древнего Рима",
    "Занимался паркуром в молодости",
    "Умеет шевелить ушами и носом",
    "Увлекается лозоходством (поиск воды)",
    "Любит разгадывать кроссворды",
    "Коллекционирует редкие монеты",
    "Увлекается стендап-комедией",
    "Занимался фехтованием на шпагах",
    "Умеет быстро собирать кубик Рубика",
    "Увлекается битбоксом",
    "Любит готовить острые соусы",
    "Увлекается спелеологией (изучение пещер)",
    "Собирает гербарий из редких трав",
    "Любит смотреть на звезды через телескоп",
    "Увлекается чечеткой",
    "Пишет стихи о постапокалипсисе",
    "Любит вязать шерстяные носки",
    "Занимается йогой на пальцах",
    "Увлекается жонглированием",
    "Любит играть в настольные игры",
    "Коллекционирует значки",
    "Увлекается гончарным делом",
    "Мечтает вывести новый сорт картофеля"
  ],
  "special1": [
    {
      "name": "Шпион",
      "action": "spy",
      "desc": "Посмотреть случайную закрытую карту любого игрока."
    },
    {
      "name": "Обмен инвентарем",
      "action": "swap_inventory",
      "desc": "Принудительно обменяться инвентарем с выбранным игроком."
    },
    {
      "name": "Двойной голос",
      "action": "double_vote",
      "desc": "Ваш голос на следующем голосовании считается за два."
    },
    {
      "name": "Лечение",
      "action": "cure_health",
      "desc": "Излечить свое здоровье или здоровье выбранного игрока."
    },
    {
      "name": "Аннулирование голоса",
      "action": "cancel_vote",
      "desc": "Лишить выбранного игрока права голоса в этом раунде."
    },
    {
      "name": "Иммунитет",
      "action": "immunity",
      "desc": "Защита от изгнания на текущем голосовании."
    },
    {
      "name": "Принудительное раскрытие",
      "action": "force_reveal",
      "desc": "Заставить любого игрока мгновенно открыть карту Профессии."
    },
    {
      "name": "Изгнание фобии",
      "action": "cure_phobia",
      "desc": "Избавить себя или другого игрока от фобии."
    },
    {
      "name": "Обмен рюкзаками",
      "action": "swap_backpack",
      "desc": "Обменяться картой 'Рюкзак' с выбранным игроком."
    },
    {
      "name": "Шпионский досмотр",
      "action": "spy",
      "desc": "Узнать здоровье и биологию любого игрока в тайне."
    },
    {
      "name": "Второй шанс",
      "action": "immunity",
      "desc": "Если вас изгонят, голосование проводится заново."
    },
    {
      "name": "Кража голоса",
      "action": "double_vote",
      "desc": "Забрать голос у выбранного игрока себе."
    },
    {
      "name": "Перетасовка",
      "action": "swap_inventory",
      "desc": "Случайный обмен картами инвентаря с соседом слева."
    },
    {
      "name": "Медицинский осмотр",
      "action": "cure_health",
      "desc": "Исправить любую болезнь у себя."
    },
    {
      "name": "Психотерапия",
      "action": "cure_phobia",
      "desc": "Вылечить фобию выбранного игрока."
    },
    {
      "name": "Рентген",
      "action": "spy",
      "desc": "Посмотреть карты 'Здоровье' у двух игроков."
    },
    {
      "name": "Кража рюкзака",
      "action": "swap_backpack",
      "desc": "Забрать рюкзак выбранного игрока, отдав свой."
    },
    {
      "name": "Ультиматум",
      "action": "cancel_vote",
      "desc": "Заблокировать возможность голосовать игроку с наибольшим числом карт."
    },
    {
      "name": "Влияние",
      "action": "double_vote",
      "desc": "Удвоить силу своего голоса против лидера."
    },
    {
      "name": "Щит бункера",
      "action": "immunity",
      "desc": "Полная неприкосновенность во время голосования."
    },
    {
      "name": "Обыск",
      "action": "spy",
      "desc": "Посмотреть все закрытые карты выбранного игрока."
    },
    {
      "name": "Конфискация",
      "action": "swap_inventory",
      "desc": "Забрать инвентарь целевого игрока."
    },
    {
      "name": "Прививка",
      "action": "cure_health",
      "desc": "Дает иммунитет к болезням и лечит текущую."
    },
    {
      "name": "Гипноз",
      "action": "cancel_vote",
      "desc": "Принудить игрока пропустить голосование."
    },
    {
      "name": "Зеркало",
      "action": "swap_backpack",
      "desc": "Поменяться всем содержимым рюкзака."
    },
    {
      "name": "Детектив",
      "action": "spy",
      "desc": "Тайный просмотр карты Спец-карты #2 у любого игрока."
    },
    {
      "name": "Провокация",
      "action": "double_vote",
      "desc": "Ваш голос весит 2 очка при голосовании."
    },
    {
      "name": "Карантин",
      "action": "cancel_vote",
      "desc": "Изолировать игрока от голосования на 1 раунд."
    },
    {
      "name": "Чудо-лекарство",
      "action": "cure_health",
      "desc": "Полное излечение здоровья любого игрока."
    },
    {
      "name": "Амнистия",
      "action": "immunity",
      "desc": "Вас нельзя выгнать в текущем раунде."
    },
    {
      "name": "Сканер",
      "action": "spy",
      "desc": "Проверить инвентарь соперника."
    },
    {
      "name": "Бартер",
      "action": "swap_inventory",
      "desc": "Поменять свой инвентарь на инвентарь любого игрока."
    },
    {
      "name": "Защитный костюм",
      "action": "cure_health",
      "desc": "Игнорировать вредные эффекты катастрофы."
    },
    {
      "name": "Голос народа",
      "action": "double_vote",
      "desc": "Дополнительный голос на выборе."
    },
    {
      "name": "Переворот",
      "action": "cancel_vote",
      "desc": "Отменить голос выбранного игрока."
    },
    {
      "name": "Запретная зона",
      "action": "immunity",
      "desc": "Защита от изгнания в этом раунде."
    },
    {
      "name": "Разведка",
      "action": "spy",
      "desc": "Узнать спец-карту соседа."
    },
    {
      "name": "Ревизия",
      "action": "swap_backpack",
      "desc": "Обменять рюкзаки с любым игроком."
    },
    {
      "name": "Психоанализ",
      "action": "cure_phobia",
      "desc": "Снять фобию у выбранного персонажа."
    },
    {
      "name": "Второстепенный маневр",
      "action": "double_vote",
      "desc": "Голос считается за два в критический момент."
    }
  ],
  "special2": [
    {
      "name": "Бункерный инженер",
      "effect": "bunker_buff",
      "desc": "Увеличивает вместимость бункера на +1 место (если возможно)."
    },
    {
      "name": "Аура здоровья",
      "effect": "passive_heal",
      "desc": "Все игроки рядом с вами получают +10% к шансу выживания."
    },
    {
      "name": "Стальные нервы",
      "effect": "phobia_immunity",
      "desc": "Вы полностью игнорируете любые панические эффекты и фобии."
    },
    {
      "name": "Мастер экономии",
      "effect": "resource_saver",
      "desc": "Запасы бункера расходуются в 2 раза медленнее."
    },
    {
      "name": "Тайный ход",
      "effect": "escape_plan",
      "desc": "Вы знаете секретный вылаз из бункера на случай ЧП."
    },
    {
      "name": "Скрытый склад",
      "effect": "extra_supplies",
      "desc": "Вы нашли дополнительный ящик с консервами на 6 месяцев."
    },
    {
      "name": "Крепкий иммунитет",
      "effect": "health_buff",
      "desc": "Вы не подвержены радиации и инфекциям."
    },
    {
      "name": "Харизматик",
      "effect": "charisma",
      "desc": "Против вас сложно голосовать: при равных голосах вы остаетесь."
    },
    {
      "name": "Автономный генератор",
      "effect": "power_gen",
      "desc": "У вас есть портативный генератор, обеспечивающий свет."
    },
    {
      "name": "Чистая вода",
      "effect": "water_filter",
      "desc": "Вы обладаете персональным портативным опреснителем."
    },
    {
      "name": "Ветеран постапокалипсиса",
      "effect": "veteran",
      "desc": "Вы знали о катастрофе заранее и подготовились."
    },
    {
      "name": "Радиолюбитель",
      "effect": "radio_contact",
      "desc": "Можете принимать сигналы из других выживших бункеров."
    },
    {
      "name": "Душа компании",
      "effect": "morale_boost",
      "desc": "Повышает моральный дух всех обитателей бункера."
    },
    {
      "name": "Золотые руки",
      "effect": "repair_master",
      "desc": "Можете починить любую поломку в бункере за 1 час."
    },
    {
      "name": "Запасливый",
      "effect": "hoarder",
      "desc": "У вас в карманах всегда найдется нужная мелочь."
    },
    {
      "name": "Ночное зрение",
      "effect": "night_vision",
      "desc": "Отлично ориентируетесь в полной темноте."
    },
    {
      "name": "Связи в правительстве",
      "effect": "insider_info",
      "desc": "Знаете точные причины и последствия катастрофы."
    },
    {
      "name": "Чутье на опасность",
      "effect": "danger_sense",
      "desc": "Заранее чувствуете приближение угроз."
    },
    {
      "name": "Дипломат",
      "effect": "diplomat",
      "desc": "Можете улаживать любые конфликты без насилия."
    },
    {
      "name": "Стрессоустойчивость",
      "effect": "calm_mind",
      "desc": "Никакие катастрофы не могут вывести вас из себя."
    },
    {
      "name": "Подземный гидропоник",
      "effect": "food_prod",
      "desc": "Умеете выращивать свежие овощи даже на бетоне."
    },
    {
      "name": "Мастер вентиляции",
      "effect": "air_expert",
      "desc": "Знаете, как очистить воздух в бункере от ядов."
    },
    {
      "name": "Оружейник",
      "effect": "weapon_master",
      "desc": "Умеете делать оружие и ловушки из хлама."
    },
    {
      "name": "Хранитель знаний",
      "effect": "scholar",
      "desc": "Помните наизусть энциклопедию выживания."
    },
    {
      "name": "Бесшумный шаг",
      "effect": "stealth",
      "desc": "Можете незаметно передвигаться вне бункера."
    },
    {
      "name": "Токсиколог",
      "effect": "poison_expert",
      "desc": "Умеете определять яды и создавать противоядия."
    },
    {
      "name": "Штурмовик",
      "effect": "brawler",
      "desc": "Способны защитить бункер от внешних нападений."
    },
    {
      "name": "Оптимист",
      "effect": "optimist",
      "desc": "Не даете другим впасть в депрессию."
    },
    {
      "name": "Быстрая регенерация",
      "effect": "fast_heal",
      "desc": "Ваши раны заживают в два раза быстрее."
    },
    {
      "name": "Эколог-эксперт",
      "effect": "eco_expert",
      "desc": "Знаете, когда снаружи станет безопасно."
    },
    {
      "name": "Сейсмолог",
      "effect": "geo_sense",
      "desc": "Заранее предсказываете подземные толчки."
    },
    {
      "name": "Холодостойкость",
      "effect": "cold_resist",
      "desc": "Легко переносите экстремально низкие температуры."
    },
    {
      "name": "Теплостойкость",
      "effect": "heat_resist",
      "desc": "Способны выдерживать жару до +60°C."
    },
    {
      "name": "Кинолог",
      "effect": "dog_whisperer",
      "desc": "Умеете приручать мутировавших животных."
    },
    {
      "name": "Электромеханик",
      "effect": "electrician",
      "desc": "Можете запустить заброшенную электростанцию."
    },
    {
      "name": "Капитан выживших",
      "effect": "leader",
      "desc": "Организуете четкое дежурство и порядок в бункере."
    },
    {
      "name": "Лекарь-травник",
      "effect": "herbalist",
      "desc": "Заменяете недостающие медикаменты травами."
    },
    {
      "name": "Хранитель семян",
      "effect": "seed_bank",
      "desc": "Владеете банком жизнеспособных семян растений."
    },
    {
      "name": "Строитель",
      "effect": "builder",
      "desc": "Умеете укреплять стены и перекрытия."
    },
    {
      "name": "Супер-выносливость",
      "effect": "stamina",
      "desc": "Можете обходиться без еды и сна до 3 суток."
    }
  ],
  "disasters": [
    {
      "title": "Ядерная зима",
      "desc": "Температура на поверхности упала до -50°C. Вся планета покрыта радиоактивным снегом."
    },
    {
      "title": "Зомби-апокалипсис",
      "desc": "Вирус Cordyceps-X превратил 99% населения в агрессивных быстрых мутантов."
    },
    {
      "title": "Вторжение инопланетных слизней",
      "desc": "Пришельцы растворяют органику и затапливают города токсичным слизким гелем."
    },
    {
      "title": "Глобальное потепление до +70°C",
      "desc": "Океаны закипели, поверхностные воды испарились. Выжить можно только глубоко под землей."
    },
    {
      "title": "Восстание умных пароварок и ИИ",
      "desc": "Бытовая техника объединилась в единый военный разум и уничтожает человечество."
    },
    {
      "title": "Пандемия смертоносного грибка",
      "desc": "Споры гриба проникают в легкие и превращают людей в спороносящие кусты."
    },
    {
      "title": "Падение гигантского астероида",
      "desc": "Удар уничтожил атмосферу, планету сотрясают землетрясения и цунами."
    },
    {
      "title": "Токсичный туман",
      "desc": "Зеленый едкий газ разъедает металл и органику за считанные минуты."
    },
    {
      "title": "Солнечная супервспышка",
      "desc": "Электромагнитный импульс сжег всю электронику, озоновый слой полностью уничтожен."
    },
    {
      "title": "Мутация флоры и фауны",
      "desc": "Растения стали хищными, а домашние животные выросли до размеров танков."
    },
    {
      "title": "Нашествие саранчи-мутантов",
      "desc": "Миллиарды бронированных насекомых пожирают древесину, пластик и людей."
    },
    {
      "title": "Геомагнитный переворот",
      "desc": "Магнитные полюса Земли сместились, смертоносная радиация заливает планету."
    },
    {
      "title": "Кислотные дожди",
      "desc": "Облака состоят из концентрированной серной кислоты. Здания рушатся."
    },
    {
      "title": "Глобальный засушливый коллапс",
      "desc": "Преломился круговорот воды, за 10 лет не выпало ни капли дождя."
    },
    {
      "title": "Техногенный выброс нанороботов",
      "desc": "Микроскопические боты разбирают всю материю на атомы ('серая слизь')."
    },
    {
      "title": "Вторжение древних монстров",
      "desc": "Из глубоких океанических впадин поднялись гигантские левиафаны."
    },
    {
      "title": "Космическая радиационная туманность",
      "desc": "Земля прошла через облако гамма-излучения. Вся живая клетка разрушается."
    },
    {
      "title": "Вулканический коллапс",
      "desc": "Одновременное извержение 50 супервулканов заблокировало солнце на 20 лет."
    },
    {
      "title": "Гравитационный сбой",
      "desc": "Гравитация стала нестабильной, предметы и люди периодически улетают в небеса."
    },
    {
      "title": "Заражение спорами из глубокого космоса",
      "desc": "Метеорит принес инопланетные споры, превращающие воду в яд."
    }
  ],
  "bunkers": [
    {
      "title": "Правительственный бункер 'Омега'",
      "desc": "Глубокий бункер с гидропоникой, душем и фильтрацией воздуха. Мест: 50% от игроков.",
      "capacityRatio": 0.5
    },
    {
      "title": "Заброшенная шахта 'Угольная'",
      "desc": "Сырые коридоры, есть рельсы и вагонетки, но туалет сломан. Мест: 50% от игроков.",
      "capacityRatio": 0.5
    },
    {
      "title": "Атомное убежище 80-х годов",
      "desc": "Толстые гермодвери, запасы тушенки на 3 года, но протекает крыша. Мест: 50% от игроков.",
      "capacityRatio": 0.5
    },
    {
      "title": "Частный бункер IT-миллиардера",
      "desc": "Роскошная отделка, спортзал, кинотеатр, но автономность всего на 1 год. Мест: 50% от игроков.",
      "capacityRatio": 0.5
    },
    {
      "title": "Подземный военный склад 'Объект 404'",
      "desc": "Много ящиков с армейскими пайками и медикаментами, но слабый генератор. Мест: 50% от игроков.",
      "capacityRatio": 0.5
    },
    {
      "title": "Переоборудованный метрополитен",
      "desc": "Просторные станции, есть чистая подземная река, но повышенная сырость. Мест: 50% от игроков.",
      "capacityRatio": 0.5
    },
    {
      "title": "Подземная лаборатория 'БиоТех'",
      "desc": "Стерильные боксы, суперчистый воздух, но ограниченные запасы еды. Мест: 50% от игроков.",
      "capacityRatio": 0.5
    },
    {
      "title": "Исследовательский бункер 'Север'",
      "desc": "Утепленные стены, отличное отопление, но нет естественного света. Мест: 50% от игроков.",
      "capacityRatio": 0.5
    },
    {
      "title": "Подземный гидропонный комплекс",
      "desc": "Свежие овощи и зелень круглый год, но мало жилых спальных мест. Мест: 50% от игроков.",
      "capacityRatio": 0.5
    },
    {
      "title": "Караван укрытий 'Ковчег'",
      "desc": "Сеть соединенных капсул с автономным питанием, но узкие проходы. Мест: 50% от игроков.",
      "capacityRatio": 0.5
    },
    {
      "title": "Старый командный пункт ПВО",
      "desc": "Мощные радары, связи с внешним миром, но барахлит система очистки воды. Мест: 50% от игроков.",
      "capacityRatio": 0.5
    },
    {
      "title": "Бункер-блокгауз 'Авангард'",
      "desc": "Бронированные стены выдержат прямой ядерный удар, но нет связи. Мест: 50% от игроков.",
      "capacityRatio": 0.5
    },
    {
      "title": "Подземный медицинский госпиталь",
      "desc": "Операционные, медикаменты, хирургические столы, но запасы пищи минимальны. Мест: 50% от игроков.",
      "capacityRatio": 0.5
    },
    {
      "title": "Бункер 'Эко-Сфера'",
      "desc": "Замкнутый замкнутая биосфера с деревьями, но сложная автоматика. Мест: 50% от игроков.",
      "capacityRatio": 0.5
    },
    {
      "title": "Глубокая винная пещера",
      "desc": "Идеальная температура, тысячи бутылок вина, но нет аптечек. Мест: 50% от игроков.",
      "capacityRatio": 0.5
    },
    {
      "title": "Подземная парковка 'Защита'",
      "desc": "Много места для хранения хлама, но вентиляция работает на 50%. Мест: 50% от игроков.",
      "capacityRatio": 0.5
    },
    {
      "title": "Секретный бункер связи 'Голос'",
      "desc": "Антенны, радиопередатчики, дизель-генераторы, но шумно. Мест: 50% от игроков.",
      "capacityRatio": 0.5
    },
    {
      "title": "Бункер солевой шахты",
      "desc": "Сухой дезинфицирующий воздух, полезно для легких, но нет пресной воды. Мест: 50% от игроков.",
      "capacityRatio": 0.5
    },
    {
      "title": "Подземный склад Росрезерва",
      "desc": "Запасы консервов на 10 лет, но слабая система освещения. Мест: 50% от игроков.",
      "capacityRatio": 0.5
    },
    {
      "title": "Экспериментальный бункер 'Сфера'",
      "desc": "Сферическая форма, выдерживает сейсмотолчки до 9 баллов, но тесные нары. Мест: 50% от игроков.",
      "capacityRatio": 0.5
    }
  ]
};
EOF

cat << 'EOF' > bunker-game/src/logic/CardGenerator.js
const cardsData = require('../data/cardsData');

class CardGenerator {
  constructor() {
    this.reset();
  }

  reset() {
    this.pools = {
      professions: [...cardsData.professions],
      health: [...cardsData.health],
      biology: [...cardsData.biology],
      inventory: [...cardsData.inventory],
      backpack: [...cardsData.backpack],
      phobias: [...cardsData.phobias],
      skills: [...cardsData.skills],
      hobbies: [...cardsData.hobbies],
      special1: [...cardsData.special1],
      special2: [...cardsData.special2]
    };
  }

  getRandomItem(category) {
    if (!this.pools[category] || this.pools[category].length === 0) {
      this.pools[category] = [...cardsData[category]];
    }
    const idx = Math.floor(Math.random() * this.pools[category].length);
    return this.pools[category].splice(idx, 1)[0];
  }

  generatePlayerCards() {
    const categories = [
      'professions', 'health', 'biology', 'inventory', 'backpack',
      'phobias', 'skills', 'hobbies', 'special1', 'special2'
    ];

    const cards = {};
    categories.forEach(cat => {
      const item = this.getRandomItem(cat);
      cards[cat] = {
        type: cat,
        value: typeof item === 'string' ? item : item.name,
        details: typeof item === 'object' ? item : null,
        revealed: false
      };
    });

    return cards;
  }

  generateDisasterAndBunker() {
    const disaster = cardsData.disasters[Math.floor(Math.random() * cardsData.disasters.length)];
    const bunker = cardsData.bunkers[Math.floor(Math.random() * cardsData.bunkers.length)];
    return { disaster, bunker };
  }
}

module.exports = CardGenerator;

EOF

cat << 'EOF' > bunker-game/src/logic/GameState.js
const CardGenerator = require('./CardGenerator');

class GameState {
  constructor(io) {
    this.io = io;
    this.status = 'LOBBY'; // LOBBY, GAME, VOTING, GAME_OVER
    this.players = new Map(); // socketId -> playerData
    this.cardGenerator = new CardGenerator();
    this.disaster = null;
    this.bunker = null;
    this.bunkerCapacity = 0;
    this.hostId = null;
    this.timer = null;
    this.timeLeft = 0;
    this.votes = new Map(); // voterSocketId -> targetSocketId
    this.logs = [];
    this.round = 1;
    this.specialModifiers = new Map(); // socketId -> { doubleVote: bool, cancelVote: bool, immunity: bool }
  }

  addPlayer(socketId, name) {
    if (this.status !== 'LOBBY') {
      return { success: false, message: 'Игра уже началась' };
    }

    if (this.players.size >= 16) {
      return { success: false, message: 'Комната заполнена (максимум 16 игроков)!' };
    }

    const player = {
      id: socketId,
      name: name.trim() || `Игрок_${socketId.substring(0, 4)}`,
      cards: null,
      eliminated: false,
      isHost: this.players.size === 0
    };

    if (player.isHost) {
      this.hostId = socketId;
    }

    this.players.set(socketId, player);
    this.addLog(`Игрок ${player.name} присоединился к игре. (${this.players.size}/16)`);
    return { success: true, player };
  }

  removePlayer(socketId) {
    const player = this.players.get(socketId);
    if (!player) return;

    this.addLog(`Игрок ${player.name} покинул игру.`);
    this.players.delete(socketId);

    if (player.isHost && this.players.size > 0) {
      const nextHost = this.players.values().next().value;
      nextHost.isHost = true;
      this.hostId = nextHost.id;
      this.addLog(`Новым хостом стал ${nextHost.name}.`);
    }

    if (this.status !== 'LOBBY' && this.getAlivePlayers().length <= this.bunkerCapacity) {
      this.checkGameOver();
    }
  }

  getAlivePlayers() {
    return Array.from(this.players.values()).filter(p => !p.eliminated);
  }

  addLog(message) {
    const time = new Date().toLocaleTimeString('ru-RU', { hour12: false });
    const logItem = `[${time}] ${message}`;
    this.logs.push(logItem);
    if (this.logs.length > 100) this.logs.shift();
    this.io.emit('log:new', logItem);
  }

  startGame() {
    if (this.players.size < 6) {
      return { success: false, message: 'Для начала игры необходимо минимум 6 игроков (сейчас: ' + this.players.size + ')!' };
    }

    this.cardGenerator.reset();
    const env = this.cardGenerator.generateDisasterAndBunker();
    this.disaster = env.disaster;
    this.bunker = env.bunker;

    const totalPlayers = this.players.size;
    this.bunkerCapacity = Math.max(1, Math.floor(totalPlayers * (this.bunker.capacityRatio || 0.5)));

    this.players.forEach(player => {
      player.cards = this.cardGenerator.generatePlayerCards();
      player.eliminated = false;
      this.specialModifiers.set(player.id, { doubleVote: false, cancelVote: false, immunity: false });
    });

    this.status = 'GAME';
    this.round = 1;
    this.addLog(`Игра началась! Участников: ${totalPlayers}. Мест в бункере: ${this.bunkerCapacity}. Катастрофа: "${this.disaster.title}".`);

    this.startDiscussionTimer();
    return { success: true };
  }

  startDiscussionTimer() {
    this.status = 'GAME';
    this.votes.clear();
    this.timeLeft = 180; // 3 minutes
    this.broadcastState();

    if (this.timer) clearInterval(this.timer);
    this.timer = setInterval(() => {
      this.timeLeft--;
      this.io.emit('timer:tick', { timeLeft: this.timeLeft, phase: 'GAME' });

      if (this.timeLeft <= 0) {
        clearInterval(this.timer);
        this.startVotingPhase();
      }
    }, 1000);
  }

  startVotingPhase() {
    this.status = 'VOTING';
    this.votes.clear();
    this.timeLeft = 30; // 30 seconds
    this.addLog(`Началось голосование на выбывание! Раунд ${this.round}. У вас 30 секунд.`);
    this.broadcastState();

    if (this.timer) clearInterval(this.timer);
    this.timer = setInterval(() => {
      this.timeLeft--;
      this.io.emit('timer:tick', { timeLeft: this.timeLeft, phase: 'VOTING' });

      if (this.timeLeft <= 0) {
        clearInterval(this.timer);
        this.processVotingResults();
      }
    }, 1000);
  }

  castVote(voterId, targetId) {
    if (this.status !== 'VOTING') return;
    const voter = this.players.get(voterId);
    const target = this.players.get(targetId);

    if (!voter || voter.eliminated) return;
    if (!target || target.eliminated) return;

    if (voterId === targetId) {
      this.io.to(voterId).emit('action:private', { title: 'Ошибка', message: 'Нельзя голосовать против самого себя!' });
      return;
    }

    const mods = this.specialModifiers.get(voterId) || {};
    if (mods.cancelVote) {
      this.io.to(voterId).emit('action:private', { title: 'Голосование заблокировано', message: 'Ваше право голоса отменено спец-картой!' });
      return;
    }

    this.votes.set(voterId, targetId);
    this.addLog(`Игрок ${voter.name} сделал свой выбор.`);
    this.broadcastVoteUpdate();

    const alivePlayers = this.getAlivePlayers();
    const validVoters = alivePlayers.filter(p => {
      const pMods = this.specialModifiers.get(p.id) || {};
      return !pMods.cancelVote;
    });

    if (this.votes.size >= validVoters.length) {
      if (this.timer) clearInterval(this.timer);
      this.processVotingResults();
    }
  }

  broadcastVoteUpdate() {
    const voteCounts = {};
    this.votes.forEach((targetId, voterId) => {
      const voterMods = this.specialModifiers.get(voterId) || {};
      const weight = voterMods.doubleVote ? 2 : 1;
      voteCounts[targetId] = (voteCounts[targetId] || 0) + weight;
    });

    this.io.emit('vote:update', {
      totalVotes: this.votes.size,
      voteCounts
    });
  }

  processVotingResults() {
    const voteCounts = {};
    this.votes.forEach((targetId, voterId) => {
      const voterMods = this.specialModifiers.get(voterId) || {};
      const weight = voterMods.doubleVote ? 2 : 1;
      voteCounts[targetId] = (voteCounts[targetId] || 0) + weight;
    });

    let maxVotes = 0;
    let candidates = [];

    for (const [targetId, count] of Object.entries(voteCounts)) {
      if (count > maxVotes) {
        maxVotes = count;
        candidates = [targetId];
      } else if (count === maxVotes && count > 0) {
        candidates.push(targetId);
      }
    }

    this.specialModifiers.forEach(mod => {
      mod.doubleVote = false;
      mod.cancelVote = false;
    });

    if (candidates.length === 0) {
      this.addLog('Никто не проголосовал! Раунд дискуссии продолжается.');
      this.round++;
      this.startDiscussionTimer();
      return;
    }

    if (candidates.length > 1) {
      this.addLog(`Ничья между кандидатами (${candidates.map(id => this.players.get(id)?.name).join(', ')}). Переголосование!`);
      this.startVotingPhase();
      return;
    }

    const eliminatedId = candidates[0];
    const eliminatedPlayer = this.players.get(eliminatedId);
    const mods = this.specialModifiers.get(eliminatedId) || {};

    if (mods.immunity) {
      mods.immunity = false;
      this.addLog(`Игрок ${eliminatedPlayer.name} был выбран на изгнание, но его защитила спец-карта Иммунитета!`);
    } else {
      eliminatedPlayer.eliminated = true;
      this.addLog(`Игрок ${eliminatedPlayer.name} был изгнан из бункера большинством голосов!`);
      this.io.emit('game:elimination', { playerId: eliminatedId, playerName: eliminatedPlayer.name });
    }

    if (this.checkGameOver()) {
      return;
    }

    this.round++;
    this.startDiscussionTimer();
  }

  checkGameOver() {
    const alive = this.getAlivePlayers();
    if (alive.length <= this.bunkerCapacity) {
      this.status = 'GAME_OVER';
      if (this.timer) clearInterval(this.timer);
      this.addLog(`Игра завершена! В бункер попали: ${alive.map(p => p.name).join(', ')}.`);
      this.broadcastState();
      return true;
    }
    return false;
  }

  revealCard(socketId, category) {
    const player = this.players.get(socketId);
    if (!player || !player.cards || !player.cards[category]) return;

    player.cards[category].revealed = true;
    const cardVal = player.cards[category].value;
    this.addLog(`Игрок ${player.name} открыл карту [${category.toUpperCase()}]: ${cardVal}`);

    this.io.emit('card:revealed', {
      playerId: socketId,
      category,
      card: player.cards[category]
    });

    this.broadcastState();
  }

  useSpecialCard(socketId, category, targetId) {
    const player = this.players.get(socketId);
    if (!player || !player.cards || !player.cards[category]) return;

    const card = player.cards[category];
    if (card.revealed) {
      this.io.to(socketId).emit('action:private', { title: 'Ошибка', message: 'Спец-карта уже использована!' });
      return;
    }

    const action = card.details ? card.details.action : null;
    const requiresTarget = ['spy', 'swap_inventory', 'swap_backpack', 'cancel_vote', 'force_reveal', 'cure_health', 'cure_phobia'].includes(action);

    if (requiresTarget && targetId === socketId) {
      this.io.to(socketId).emit('action:private', { title: 'Ошибка', message: 'Эту способность нельзя применять на самого себя!' });
      return;
    }

    card.revealed = true;
    const target = this.players.get(targetId);

    this.addLog(`Игрок ${player.name} применил спец-карту "${card.value}"!`);

    if (action === 'spy' && target) {
      const targetCards = target.cards;
      const unrevealedCats = Object.keys(targetCards).filter(c => !targetCards[c].revealed);
      const chosenCat = unrevealedCats.length > 0 ? unrevealedCats[Math.floor(Math.random() * unrevealedCats.length)] : 'health';
      const spyCard = targetCards[chosenCat];

      this.io.to(socketId).emit('action:private', {
        title: `Шпионаж: ${target.name}`,
        message: `Карта [${chosenCat.toUpperCase()}]: ${spyCard.value}${spyCard.details ? ' (' + (spyCard.details.desc || spyCard.details) + ')' : ''}`
      });
    } else if (action === 'swap_inventory' && target) {
      const temp = player.cards.inventory;
      player.cards.inventory = target.cards.inventory;
      target.cards.inventory = temp;
      this.addLog(`Игрок ${player.name} поменялся инвентарем с ${target.name}!`);
    } else if (action === 'swap_backpack' && target) {
      const temp = player.cards.backpack;
      player.cards.backpack = target.cards.backpack;
      target.cards.backpack = temp;
      this.addLog(`Игрок ${player.name} поменялся рюкзаком с ${target.name}!`);
    } else if (action === 'double_vote') {
      const mods = this.specialModifiers.get(socketId);
      if (mods) mods.doubleVote = true;
      this.io.to(socketId).emit('action:private', { title: 'Эффект карты', message: 'Ваш следующий голос будет посчитан за два!' });
    } else if (action === 'cure_health') {
      const targetPlayer = target || player;
      targetPlayer.cards.health.value = 'Абсолютно здоров (Излечен)';
      targetPlayer.cards.health.revealed = true;
      this.addLog(`Игрок ${targetPlayer.name} полностью излечен!`);
    } else if (action === 'cure_phobia') {
      const targetPlayer = target || player;
      targetPlayer.cards.phobias.value = 'Фобия отсутствует (Излечен)';
      targetPlayer.cards.phobias.revealed = true;
      this.addLog(`Игрок ${targetPlayer.name} избавлен от фобии!`);
    } else if (action === 'cancel_vote' && target) {
      const mods = this.specialModifiers.get(target.id);
      if (mods) mods.cancelVote = true;
      this.addLog(`Игрок ${target.name} лишен права голоса на ближайшем голосовании!`);
    } else if (action === 'immunity') {
      const mods = this.specialModifiers.get(socketId);
      if (mods) mods.immunity = true;
      this.io.to(socketId).emit('action:private', { title: 'Иммунитет', message: 'Вы получили защиту от изгнания в текущем раунде!' });
    } else if (action === 'force_reveal' && target) {
      target.cards.professions.revealed = true;
      this.addLog(`Игрок ${target.name} был принужден раскрыть категорию Профессия!`);
    }

    this.broadcastState();
  }

  getSanitizedState(forSocketId) {
    const playersList = Array.from(this.players.values()).map(p => {
      const isSelf = p.id === forSocketId;
      const sanitizedCards = {};

      if (p.cards) {
        Object.keys(p.cards).forEach(cat => {
          const card = p.cards[cat];
          if (isSelf || card.revealed) {
            sanitizedCards[cat] = card;
          } else {
            sanitizedCards[cat] = {
              type: cat,
              value: '??? (Скрыто)',
              revealed: false
            };
          }
        });
      }

      return {
        id: p.id,
        name: p.name,
        eliminated: p.eliminated,
        isHost: p.isHost,
        cards: sanitizedCards
      };
    });

    return {
      status: this.status,
      disaster: this.disaster,
      bunker: this.bunker,
      bunkerCapacity: this.bunkerCapacity,
      timeLeft: this.timeLeft,
      round: this.round,
      hostId: this.hostId,
      players: playersList,
      logs: this.logs
    };
  }

  broadcastState() {
    this.players.forEach((player, socketId) => {
      this.io.to(socketId).emit('room:updated', this.getSanitizedState(socketId));
    });
  }
}

module.exports = GameState;

EOF

cat << 'EOF' > bunker-game/server.js
const express = require('express');
const http = require('http');
const { Server } = require('socket.io');
const os = require('os');
const path = require('path');
const GameState = require('./src/logic/GameState');

const app = express();
const server = http.createServer(app);
const io = new Server(server, {
  cors: {
    origin: "*",
    methods: ["GET", "POST"]
  }
});

app.use(express.static(path.join(__dirname, 'public')));

const gameState = new GameState(io);

function getLocalIp() {
  const interfaces = os.networkInterfaces();
  for (const name of Object.keys(interfaces)) {
    for (const iface of interfaces[name]) {
      if (iface.family === 'IPv4' && !iface.internal) {
        return iface.address;
      }
    }
  }
  return 'localhost';
}

io.on('connection', (socket) => {
  console.log(`[Socket] Новое подключение: ${socket.id}`);

  socket.on('room:join', ({ name }) => {
    const result = gameState.addPlayer(socket.id, name);
    if (result.success) {
      socket.emit('game:init', gameState.getSanitizedState(socket.id));
      gameState.broadcastState();
    } else {
      socket.emit('error:msg', result.message);
    }
  });

  socket.on('game:start', () => {
    const player = gameState.players.get(socket.id);
    if (player && player.isHost) {
      const res = gameState.startGame();
      if (!res.success) {
        socket.emit('error:msg', res.message);
      }
    }
  });

  socket.on('game:next_phase', () => {
    const player = gameState.players.get(socket.id);
    if (player && player.isHost) {
      if (gameState.status === 'GAME') {
        gameState.startVotingPhase();
      } else if (gameState.status === 'VOTING') {
        gameState.processVotingResults();
      }
    }
  });

  socket.on('card:reveal', ({ category }) => {
    gameState.revealCard(socket.id, category);
  });

  socket.on('card:action', ({ category, targetId }) => {
    gameState.useSpecialCard(socket.id, category, targetId);
  });

  socket.on('vote:cast', ({ targetId }) => {
    gameState.castVote(socket.id, targetId);
  });

  socket.on('disconnect', () => {
    console.log(`[Socket] Игрок отключился: ${socket.id}`);
    gameState.removePlayer(socket.id);
    gameState.broadcastState();
  });
});

const PORT = process.env.PORT || 3000;
const HOST = '0.0.0.0';
const LOCAL_IP = getLocalIp();

if (require.main === module) {
  server.listen(PORT, HOST, () => {
    console.log('====================================================');
    console.log('   🔥 ПОСТАПОКАЛИПТИЧЕСКАЯ ВЕБ-ИГРА «БУНКЕР» 🔥     ');
    console.log('====================================================');
    console.log(` Сервер запущен на хосте: ${HOST}:${PORT}`);
    console.log(` 🌐 Игра доступна по адресу: http://${LOCAL_IP}:${PORT}`);
    console.log('====================================================');
  });
}

module.exports = app;

EOF

cat << 'EOF' > bunker-game/public/index.html
<!DOCTYPE html>
<html lang="ru" class="dark h-full">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Игра «Бункер» — Постапокалипсис (Онлайн)</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="/socket.io/socket.io.js"></script>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-gray-900 text-gray-100 h-full flex flex-col font-sans select-none overflow-hidden">

  <!-- JOIN MODAL -->
  <div id="join-modal" class="fixed inset-0 bg-black bg-opacity-90 z-50 flex items-center justify-center p-4">
    <div class="bg-gray-800 border border-amber-600 rounded-xl p-6 max-w-md w-full shadow-2xl text-center space-y-5">
      <div class="text-4xl">☢️</div>
      <h1 class="text-3xl font-extrabold text-amber-500 tracking-wider">БУНКЕР ОНЛАЙН</h1>
      <p class="text-gray-400 text-sm">От 6 до 16 игроков. Введите ваш никнейм:</p>
      <input type="text" id="player-name-input" placeholder="Ваше имя..." class="w-full px-4 py-3 bg-gray-900 border border-gray-700 rounded-lg focus:outline-none focus:border-amber-500 text-center text-lg text-white">
      <button id="join-btn" class="w-full py-3 bg-amber-600 hover:bg-amber-500 text-black font-bold text-lg rounded-lg transition duration-200 shadow-lg">ВОЙТИ В ИГРУ</button>
    </div>
  </div>

  <!-- ACTION TARGET SELECTION MODAL -->
  <div id="action-modal" class="hidden fixed inset-0 bg-black bg-opacity-80 z-40 flex items-center justify-center p-4">
    <div class="bg-gray-800 border border-amber-500 rounded-xl p-6 max-w-md w-full text-center space-y-4">
      <h3 id="action-modal-title" class="text-xl font-bold text-amber-400">Выберите другого игрока</h3>
      <div id="action-target-list" class="space-y-2 max-h-60 overflow-y-auto"></div>
      <button id="action-modal-cancel" class="w-full py-2 bg-gray-700 hover:bg-gray-600 rounded-lg text-gray-300 font-semibold">Отмена</button>
    </div>
  </div>

  <!-- PRIVATE MESSAGE MODAL -->
  <div id="private-modal" class="hidden fixed inset-0 bg-black bg-opacity-80 z-50 flex items-center justify-center p-4">
    <div class="bg-gray-800 border border-yellow-500 rounded-xl p-6 max-w-md w-full text-center space-y-4 shadow-2xl">
      <h3 id="private-modal-title" class="text-xl font-bold text-yellow-400">🕵️ Уведомление</h3>
      <p id="private-modal-body" class="text-gray-200 text-base leading-relaxed bg-gray-900 p-4 rounded-lg border border-gray-700"></p>
      <button id="private-modal-close" class="w-full py-2 bg-amber-600 hover:bg-amber-500 font-bold rounded-lg text-black">ПОНЯТНО</button>
    </div>
  </div>

  <!-- HEADER -->
  <header class="bg-gray-950 border-b border-gray-800 px-4 py-2 flex items-center justify-between shrink-0 h-14">
    <div class="flex items-center space-x-3">
      <span class="text-2xl">☢️</span>
      <h1 class="text-lg font-bold text-amber-500 tracking-widest hidden sm:inline">БУНКЕР</h1>
      <span id="phase-badge" class="px-2.5 py-1 text-xs font-semibold rounded-full bg-gray-800 text-gray-300 border border-gray-700">ОЖИДАНИЕ</span>
    </div>
    <div class="flex items-center space-x-4">
      <div id="timer-box" class="hidden text-amber-400 font-mono font-bold text-lg bg-gray-900 border border-amber-900/50 px-3 py-1 rounded-lg">⏱️ <span id="timer-text">03:00</span></div>
      <button id="host-start-btn" class="hidden px-4 py-1.5 bg-emerald-600 hover:bg-emerald-500 text-white font-bold text-sm rounded-lg shadow transition">НАЧАТЬ ИГРУ (6+)</button>
      <button id="host-next-btn" class="hidden px-4 py-1.5 bg-amber-600 hover:bg-amber-500 text-black font-bold text-sm rounded-lg shadow transition">СЛЕД. ФАЗА</button>
    </div>
  </header>

  <!-- MAIN DASHBOARD -->
  <main class="flex-1 flex flex-col md:flex-row overflow-hidden relative">

    <!-- SECTION 1: MY CARDS -->
    <section id="tab-my-cards" class="tab-content active-tab flex-1 flex flex-col p-3 border-r border-gray-800 overflow-y-auto">
      <h2 class="text-md font-bold text-amber-500 mb-2 flex items-center justify-between shrink-0">
        <span>🃏 Мои характеристики</span>
        <span id="my-status-tag" class="text-xs text-gray-400 font-normal">Нажмите "Открыть" для показа другим</span>
      </h2>
      <div id="my-cards-grid" class="grid grid-cols-2 sm:grid-cols-2 lg:grid-cols-5 gap-2.5">
      </div>
    </section>

    <!-- SECTION 2: BUNKER & DISASTER -->
    <section id="tab-bunker" class="tab-content hidden md:flex w-full md:w-80 flex-col p-3 border-r border-gray-800 shrink-0 space-y-3 overflow-y-auto bg-gray-900/50">
      <div>
        <h3 class="text-sm font-bold text-red-500 mb-1 tracking-wide">💥 КАТАСТРОФА</h3>
        <div id="disaster-card" class="bg-gray-800/80 border border-red-900/50 p-3 rounded-lg text-xs space-y-1">
          <p class="font-bold text-red-400 text-sm" id="disaster-title">Ожидание старта...</p>
          <p class="text-gray-300 leading-relaxed" id="disaster-desc">Катастрофа еще не определена.</p>
        </div>
      </div>
      <div>
        <h3 class="text-sm font-bold text-emerald-500 mb-1 tracking-wide">🛡️ СОСТОЯНИЕ БУНКЕРА</h3>
        <div id="bunker-card" class="bg-gray-800/80 border border-emerald-900/50 p-3 rounded-lg text-xs space-y-2">
          <p class="font-bold text-emerald-400 text-sm" id="bunker-title">Убежище не выбрано</p>
          <p class="text-gray-300 leading-relaxed" id="bunker-desc">Параметры бункера будут раскрыты после запуска.</p>
          <div class="pt-2 border-t border-gray-700 flex justify-between text-gray-400">
            <span>Спасительных мест:</span>
            <span id="bunker-capacity" class="font-bold text-amber-400">0</span>
          </div>
        </div>
      </div>
      <!-- LOGS PANEL -->
      <div class="flex-1 flex flex-col min-h-[140px]">
        <h3 class="text-sm font-bold text-gray-400 mb-1">📜 Лог событий</h3>
        <div id="game-logs" class="flex-1 bg-gray-950 border border-gray-800 rounded-lg p-2 text-[11px] font-mono text-gray-300 overflow-y-auto space-y-1"></div>
      </div>
    </section>

    <!-- SECTION 3: PLAYERS -->
    <section id="tab-players" class="tab-content hidden md:flex w-full md:w-80 flex-col p-3 shrink-0 overflow-y-auto">
      <h2 class="text-md font-bold text-amber-500 mb-2 flex items-center justify-between shrink-0">
        <span>👥 Выжившие (6-16)</span>
        <span id="players-count" class="text-xs text-gray-400">0 человек</span>
      </h2>
      <div id="players-list" class="space-y-2 overflow-y-auto flex-1"></div>
    </section>

  </main>

  <!-- MOBILE BOTTOM NAVIGATION -->
  <nav class="md:hidden bg-gray-950 border-t border-gray-800 flex justify-around p-2 shrink-0 h-14">
    <button data-tab="tab-my-cards" class="mobile-nav-btn active flex-1 flex flex-col items-center justify-center text-amber-500">
      <span class="text-lg">🃏</span>
      <span class="text-[10px] font-medium">Мои карты</span>
    </button>
    <button data-tab="tab-bunker" class="mobile-nav-btn flex-1 flex flex-col items-center justify-center text-gray-400">
      <span class="text-lg">🛡️</span>
      <span class="text-[10px] font-medium">Бункер</span>
    </button>
    <button data-tab="tab-players" class="mobile-nav-btn flex-1 flex flex-col items-center justify-center text-gray-400">
      <span class="text-lg">👥</span>
      <span class="text-[10px] font-medium">Игроки</span>
    </button>
  </nav>

  <script src="/js/ui.js"></script>
  <script src="/js/socket-handler.js"></script>
  <script src="/js/app.js"></script>
</body>
</html>

EOF

cat << 'EOF' > bunker-game/public/css/style.css
/* Custom styling for dark post-apocalyptic bunker interface */
body {
  user-select: none;
  touch-action: manipulation;
}

::-webkit-scrollbar {
  width: 6px;
  height: 6px;
}
::-webkit-scrollbar-track {
  background: #0f172a;
}
::-webkit-scrollbar-thumb {
  background: #334155;
  border-radius: 3px;
}
::-webkit-scrollbar-thumb:hover {
  background: #475569;
}

.mobile-nav-btn.active {
  color: #f59e0b;
}

.card-item {
  transition: transform 0.15s ease, border-color 0.15s ease;
}
.card-item:hover {
  transform: translateY(-2px);
}

EOF

cat << 'EOF' > bunker-game/public/js/socket-handler.js
const socket = io();

const SocketHandler = {
  joinRoom(name) {
    socket.emit('room:join', { name });
  },

  startGame() {
    socket.emit('game:start');
  },

  nextPhase() {
    socket.emit('game:next_phase');
  },

  revealCard(category) {
    socket.emit('card:reveal', { category });
  },

  useCardAction(category, targetId) {
    socket.emit('card:action', { category, targetId });
  },

  castVote(targetId) {
    socket.emit('vote:cast', { targetId });
  },

  initListeners() {
    socket.on('game:init', (state) => {
      UI.updateGameState(state);
    });

    socket.on('room:updated', (state) => {
      UI.updateGameState(state);
    });

    socket.on('timer:tick', (data) => {
      UI.updateTimer(data.timeLeft);
    });

    socket.on('vote:update', (data) => {
      UI.updateVoteCounts(data.voteCounts);
    });

    socket.on('action:private', (data) => {
      UI.showPrivateModal(data.title, data.message);
    });

    socket.on('log:new', (logMessage) => {
      UI.appendLog(logMessage);
    });

    socket.on('error:msg', (msg) => {
      alert(msg);
    });
  }
};

EOF

cat << 'EOF' > bunker-game/public/js/ui.js
const categoryNames = {
  professions: 'Профессия',
  health: 'Здоровье',
  biology: 'Биология / Пол',
  inventory: 'Инвентарь',
  backpack: 'Рюкзак',
  phobias: 'Фобия',
  skills: 'Навык (Доп. 1)',
  hobbies: 'Хобби (Доп. 2)',
  special1: 'Спец-карта #1',
  special2: 'Спец-карта #2'
};

const UI = {
  currentState: null,
  selectedCategoryForAction: null,

  init() {
    this.setupMobileTabs();
  },

  setupMobileTabs() {
    const btns = document.querySelectorAll('.mobile-nav-btn');
    btns.forEach(btn => {
      btn.addEventListener('click', () => {
        btns.forEach(b => {
          b.classList.remove('active');
          b.classList.replace('text-amber-500', 'text-gray-400');
        });
        btn.classList.add('active');
        btn.classList.replace('text-gray-400', 'text-amber-500');

        const targetTab = btn.getAttribute('data-tab');
        document.querySelectorAll('.tab-content').forEach(content => {
          if (content.id === targetTab) {
            content.classList.remove('hidden');
            content.classList.add('flex');
          } else {
            content.classList.add('hidden');
            content.classList.remove('flex');
          }
        });
      });
    });
  },

  updateGameState(state) {
    this.currentState = state;

    const badge = document.getElementById('phase-badge');
    if (state.status === 'LOBBY') badge.innerText = `Лобби (${state.players.length}/16)`;
    else if (state.status === 'GAME') badge.innerText = `Раунд ${state.round}: Дискуссия`;
    else if (state.status === 'VOTING') badge.innerText = `Раунд ${state.round}: Голосование`;
    else if (state.status === 'GAME_OVER') badge.innerText = 'Игра Завершена';

    const isHost = socket.id === state.hostId;
    const startBtn = document.getElementById('host-start-btn');
    const nextBtn = document.getElementById('host-next-btn');

    if (isHost && state.status === 'LOBBY') {
      startBtn.classList.remove('hidden');
    } else {
      startBtn.classList.add('hidden');
    }

    if (isHost && (state.status === 'GAME' || state.status === 'VOTING')) {
      nextBtn.classList.remove('hidden');
    } else {
      nextBtn.classList.add('hidden');
    }

    const timerBox = document.getElementById('timer-box');
    if (state.status !== 'LOBBY' && state.status !== 'GAME_OVER') {
      timerBox.classList.remove('hidden');
    } else {
      timerBox.classList.add('hidden');
    }

    if (state.disaster) {
      document.getElementById('disaster-title').innerText = state.disaster.title;
      document.getElementById('disaster-desc').innerText = state.disaster.desc;
    }
    if (state.bunker) {
      document.getElementById('bunker-title').innerText = state.bunker.title;
      document.getElementById('bunker-desc').innerText = state.bunker.desc;
      document.getElementById('bunker-capacity').innerText = `${state.bunkerCapacity} чел.`;
    }

    const me = state.players.find(p => p.id === socket.id);
    this.renderMyCards(me);
    this.renderPlayersList(state.players, me);
    this.renderLogs(state.logs);
  },

  renderMyCards(me) {
    const grid = document.getElementById('my-cards-grid');
    grid.innerHTML = '';

    if (!me || !me.cards) {
      grid.innerHTML = '<div class="col-span-full text-center text-gray-500 py-8">Ожидание раздачи карт...</div>';
      return;
    }

    Object.keys(me.cards).forEach(cat => {
      const card = me.cards[cat];
      const cardEl = document.createElement('div');
      const isRevealed = card.revealed;
      const isSpecial1 = cat === 'special1';

      cardEl.className = `card-item border rounded-lg p-2.5 flex flex-col justify-between text-xs transition duration-200 ${
        isRevealed ? 'bg-gray-800/90 border-amber-600/70' : 'bg-gray-950 border-gray-800 hover:border-gray-700'
      }`;

      let actionsHtml = '';
      if (!isRevealed) {
        if (isSpecial1) {
          actionsHtml = `<button onclick="UI.handleSpecialCardClick('${cat}')" class="mt-2 w-full py-1 bg-purple-700 hover:bg-purple-600 font-bold rounded text-white text-[11px]">Применить</button>`;
        } else {
          actionsHtml = `<button onclick="SocketHandler.revealCard('${cat}')" class="mt-2 w-full py-1 bg-amber-600 hover:bg-amber-500 font-bold rounded text-black text-[11px]">Открыть</button>`;
        }
      } else {
        actionsHtml = `<span class="mt-2 text-[10px] text-amber-500/80 font-mono text-center">Открыто</span>`;
      }

      cardEl.innerHTML = `
        <div>
          <div class="text-[10px] uppercase font-bold text-gray-400 tracking-wider mb-1">${categoryNames[cat] || cat}</div>
          <div class="font-bold text-amber-400 text-xs leading-snug mb-1">${card.value}</div>
          ${card.details && card.details.desc ? `<p class="text-[10px] text-gray-400 italic">${card.details.desc}</p>` : ''}
        </div>
        ${actionsHtml}
      `;

      grid.appendChild(cardEl);
    });
  },

  handleSpecialCardClick(cat) {
    this.selectedCategoryForAction = cat;
    const me = this.currentState.players.find(p => p.id === socket.id);
    if (!me || !me.cards || !me.cards[cat]) return;

    const action = me.cards[cat].details ? me.cards[cat].details.action : null;

    // Self-only / Non-targeted abilities execute directly on self
    if (action === 'double_vote' || action === 'immunity') {
      SocketHandler.useCardAction(cat, socket.id);
      return;
    }

    // Targeted abilities MUST select ANOTHER player (self excluded!)
    const modal = document.getElementById('action-modal');
    const targetList = document.getElementById('action-target-list');
    targetList.innerHTML = '';

    // Filter out self so player CANNOT target themselves
    const otherAlivePlayers = this.currentState.players.filter(p => !p.eliminated && p.id !== socket.id);

    if (otherAlivePlayers.length === 0) {
      alert('Нет других доступных игроков для применения способности!');
      return;
    }

    otherAlivePlayers.forEach(p => {
      const btn = document.createElement('button');
      btn.className = 'w-full py-2 bg-gray-700 hover:bg-amber-600 hover:text-black rounded text-sm text-gray-200 font-medium transition';
      btn.innerText = p.name;
      btn.onclick = () => {
        SocketHandler.useCardAction(cat, p.id);
        modal.classList.add('hidden');
      };
      targetList.appendChild(btn);
    });

    modal.classList.remove('hidden');
  },

  renderPlayersList(players, me) {
    const list = document.getElementById('players-list');
    list.innerHTML = '';

    document.getElementById('players-count').innerText = `${players.filter(p => !p.eliminated).length} / ${players.length}`;

    players.forEach(p => {
      const isSelf = p.id === socket.id;
      const isVoting = this.currentState.status === 'VOTING' && me && !me.eliminated && !p.eliminated;

      const pEl = document.createElement('div');
      pEl.className = `p-2.5 rounded-lg border text-xs space-y-1.5 ${
        p.eliminated ? 'bg-red-950/20 border-red-900/40 opacity-60' : isSelf ? 'bg-amber-950/20 border-amber-600/60' : 'bg-gray-800/60 border-gray-700'
      }`;

      let cardsHtml = '';
      if (p.cards) {
        cardsHtml = Object.keys(p.cards).map(cat => {
          const card = p.cards[cat];
          return `<span class="inline-block px-1.5 py-0.5 rounded text-[10px] ${
            card.revealed ? 'bg-amber-900/40 text-amber-300 border border-amber-700/50' : 'bg-gray-900 text-gray-500'
          }">${categoryNames[cat]}: ${card.value}</span>`;
        }).join(' ');
      }

      let voteBtnHtml = '';
      if (isVoting && !isSelf) {
        voteBtnHtml = `<button onclick="SocketHandler.castVote('${p.id}')" class="px-2 py-1 bg-red-700 hover:bg-red-600 font-bold text-white rounded text-[10px]">Голосовать</button>`;
      }

      pEl.innerHTML = `
        <div class="flex items-center justify-between">
          <span class="font-bold text-sm ${p.eliminated ? 'line-through text-red-500' : 'text-gray-200'}">
            ${p.name} ${p.isHost ? '👑' : ''} ${isSelf ? '(Вы)' : ''}
          </span>
          <div class="flex items-center space-x-2">
            <span id="vote-count-${p.id}" class="text-xs font-bold text-red-400"></span>
            ${voteBtnHtml}
          </div>
        </div>
        <div class="flex flex-wrap gap-1 pt-1 border-t border-gray-700/50">
          ${cardsHtml}
        </div>
      `;

      list.appendChild(pEl);
    });
  },

  updateVoteCounts(voteCounts) {
    if (!voteCounts) return;
    Object.keys(voteCounts).forEach(targetId => {
      const badge = document.getElementById(`vote-count-${targetId}`);
      if (badge) {
        badge.innerText = `🗳️ ${voteCounts[targetId]}`;
      }
    });
  },

  updateTimer(timeLeft) {
    const mins = Math.floor(timeLeft / 60).toString().padStart(2, '0');
    const secs = (timeLeft % 60).toString().padStart(2, '0');
    document.getElementById('timer-text').innerText = `${mins}:${secs}`;
  },

  showPrivateModal(title, message) {
    document.getElementById('private-modal-title').innerText = title;
    document.getElementById('private-modal-body').innerText = message;
    document.getElementById('private-modal').classList.remove('hidden');
  },

  renderLogs(logs) {
    const logContainer = document.getElementById('game-logs');
    if (!logContainer) return;
    logContainer.innerHTML = logs.map(l => `<div>${l}</div>`).join('');
    logContainer.scrollTop = logContainer.scrollHeight;
  },

  appendLog(logMessage) {
    const logContainer = document.getElementById('game-logs');
    if (!logContainer) return;
    const div = document.createElement('div');
    div.innerText = logMessage;
    logContainer.appendChild(div);
    logContainer.scrollTop = logContainer.scrollHeight;
  }
};

EOF

cat << 'EOF' > bunker-game/public/js/app.js
document.addEventListener('DOMContentLoaded', () => {
  UI.init();
  SocketHandler.initListeners();

  const joinBtn = document.getElementById('join-btn');
  const nameInput = document.getElementById('player-name-input');
  const joinModal = document.getElementById('join-modal');

  const doJoin = () => {
    const name = nameInput.value.trim();
    if (name) {
      SocketHandler.joinRoom(name);
      joinModal.classList.add('hidden');
    } else {
      alert('Пожалуйста, введите никнейм');
    }
  };

  joinBtn.addEventListener('click', doJoin);
  nameInput.addEventListener('keypress', (e) => {
    if (e.key === 'Enter') doJoin();
  });

  document.getElementById('host-start-btn').addEventListener('click', () => {
    SocketHandler.startGame();
  });

  document.getElementById('host-next-btn').addEventListener('click', () => {
    SocketHandler.nextPhase();
  });

  document.getElementById('action-modal-cancel').addEventListener('click', () => {
    document.getElementById('action-modal').classList.add('hidden');
  });

  document.getElementById('private-modal-close').addEventListener('click', () => {
    document.getElementById('private-modal').classList.add('hidden');
  });
});

EOF

echo "✅ Проект успешно создан в директории bunker-game/"
echo "📦 Установка зависимостей..."
cd bunker-game
npm install
echo "=========================================="
echo "🎉 Проект готов к запуску и деплою на Vercel!"
echo "Для локального запуска используйте: cd bunker-game && npm start"
echo "=========================================="
